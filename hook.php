<?php
function plugin_workorderprint_install() {
   //do some stuff like instanciating databases, default values, ...
   return true;
}

/**
 * Uninstall hook
 *
 * @return boolean
 */
function plugin_workorderprint_uninstall() {
   //to some stuff, like removing tables, generated files, ...
   return true;
}
